[build]
command = "npx vite build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"

# SPA redirects - all routes should serve index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200